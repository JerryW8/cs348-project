<!doctype html>

<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Our main page">
    <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js" integrity="sha384-KJ3o2DKtIkvYIK3UENzmM7KCkRr/rE9/Qpg6aAZGJwFDMVNA/GpGFF93hXpG5KkN" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.12.9/dist/umd/popper.min.js" integrity="sha384-ApNbgh9B+Y1QKtv3Rn7W3mgPxhU9K/ScQsAP7hUibX39j7fakFPskvXusvfa0b4Q" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>
    <link href="/css/styles.css" rel="stylesheet" type="text/css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <title>CS 348 IMDB Project</title>
</head>


<body>
    <div class="media-title">
        <center> 
            <h2><label for="media"><a href="/media/<%= media.titleID %>"> <%- media.originalTitle %> </a></label></h2><br>
        </center>
    </div>
    <div class="media-info">
        <% if(media.isWatched) { %>
            You have watched this movie
        <% } else { %>
            You have not watched this movie
        <% } %>
        <br>
        <% if(!media.rating) { %>
            You have not given it a rating
        <% } else { %>
            You rated this movie a <%- media.rating %>
        <% } %>
        <br><br>
        Your Notes:
        <br>
        <%- media.notes %>
        <br>
        <br>
        <!-- Button trigger modal -->
        <button type="button" class="btn btn-secondary" data-toggle="modal" data-target="#editModal">
            Edit
        </button>
        <br>
        <br>
        <form action="/collection/<%= media.collectionID %>/delete" method="post">
            <input type="submit" type="button" class="btn btn-danger" value="Delete from collection">
        </form>
        
        <!-- Modal -->
        <div class="modal fade" id="editModal" tabindex="-1" role="dialog" aria-labelledby="editModalTitle" aria-hidden="true">
            <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                <h5 class="modal-title" id="editModaTitle">Edit Collection Info for <%- media.originalTitle %></h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
                </div>
                <form action="/collection/<%= media.collectionID %>/update" method="post">
                    <div class="modal-body">
                        <h6> Watched? </h6>
                        <input type="radio" id="yes" name="isWatched" value="true">
                        <label for="yes">Yes</label>
                        <br>
                        <input type="radio" id="no" name="isWatched" value="false">
                        <label for="no">No</label>
                        <script>
                            let m = <%= media.isWatched %>
                            if (m) {
                                document.getElementById("yes").checked = true
                            } else {
                                document.getElementById("no").checked = true
                            }
                        </script>
                        <br>
                        <br>
                        <label for="rating"><h6>Your rating: </h6></label>
                        <br>
                        <input type="text" id="rating" name="rating" maxlength="3" value="<%= media.rating %>">
                        <br>
                        <br>
                        <label for="notes"><h6>Your notes: </h6></label>
                        <br>
                        <textarea id="notes" name="notes" rows="5" cols="50" maxlength="1000"><%= media.notes %></textarea>
                    </div>
                    <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <input type="submit" id="submit-changes" type="button" class="btn btn-primary" value="Save changes">
                    <script>
                        ratingField = document.getElementById("rating")
                        submitButton = document.getElementById("submit-changes")
                        ratingField.addEventListener("input", () => {
                            let input = ratingField.value
                            length = input.length
                            if (length == 0 || length == 1 && '0' <= input.charAt(0) && input.charAt(0) <= '9') {
                                submitButton.disabled = false
                            } else if (length == 3 && '0' <= input.charAt(0) && input.charAt(0) <= '9' && input.charAt(1) == '.' && '0' <= input.charAt(2) && input.charAt(2) <= '9') {
                                submitButton.disabled = false
                            } else {
                                submitButton.disabled = true
                            }
                        })
                    </script>
                </form>
                </div>
            </div>
            </div>
        </div>
    </div>
</body>

</html>